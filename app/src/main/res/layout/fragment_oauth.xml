<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/toolbar_oauth"
            layout="@layout/view_toolbar" />

        <com.jtech.view.JRecyclerView
            android:id="@id/jrecyclerview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/toolbar_oauth"
            android:paddingLeft="@dimen/recyclerview_default_margin"
            android:paddingRight="@dimen/recyclerview_default_margin" />

        <WebView
            android:id="@id/webview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/toolbar_oauth"
            android:visibility="invisible" />

        <android.support.v4.widget.ContentLoadingProgressBar
            android:id="@id/contentloading"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="@dimen/progress_height"
            android:layout_below="@+id/toolbar_oauth"
            android:progress="0" />
    </RelativeLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/fab_default_margin"
        android:src="@drawable/ic_done_white_36dp"
        android:transitionName="@string/fab"
        app:layout_anchor="@id/jrecyclerview"
        app:layout_anchorGravity="bottom|center_horizontal"
        app:useCompatPadding="true" />

</android.support.design.widget.CoordinatorLayout>